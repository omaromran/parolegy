# Database (required)
# Local: SQLite (no setup)
DATABASE_URL="file:./dev.db"
# Production: use PostgreSQL and set DATABASE_URL in your host's env vars
# DATABASE_URL="postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require"

# Auth (required for signup/login)
NEXTAUTH_SECRET="your-secret-key-here-change-in-production"

# Optional: site password protection
# SITE_PASSWORD="your-password"

# OpenAI (required for campaign generation - self-serve)
OPENAI_API_KEY="sk-your-openai-api-key-here"
# Optional: model (defaults to gpt-4-turbo-preview)
# OPENAI_MODEL="gpt-4-turbo-preview"
